import React, { Fragment } from 'react';

import ImageSlider from '../../components/ImageSlider/ImageSlider';

const Home = props => {
    return (
        <Fragment>
            <ImageSlider/>
        </Fragment>
    )
}

export default Home;